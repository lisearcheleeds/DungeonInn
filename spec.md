# DungeonInn ゲーム仕様書

バージョン: 1.0.0  
対象フレームワーク: Lighthouse (VContainer / UniTask / R3)  
Unity バージョン: Unity 6 (URP)

---

## 1. ゲームコンセプト

ダンジョン入口付近の村で、冒険者向けの宿屋を経営するシミュレーションゲーム。  
プレイヤーは宿屋の店長として資金を稼ぎ、土地を拡張し、冒険者の満足度を高めながら宿屋を発展させる。

---

## 2. ワールド設計

### 2.1 グリッドシステム

- **基本単位**: 1ブロック = 1m の立方体
- **地形・建築物**: すべてグリッド座標で位置を決定
- **動的オブジェクト（キャラクター等）**: グリッドに拘束されず自由移動
- **デフォルトマップサイズ**: 1000 × 1000 × 1000 ブロック
- **マップサイズ**: 設定ファイルで自由に変更可能（`WorldConfig` で管理）

### 2.2 レイヤー構造

| レイヤー名 | Y座標範囲 | 説明 |
|-----------|-----------|------|
| 地上 | Y >= 0 | 村・宿屋エリア |
| 地下1階 | Y = -1〜-20 | ダンジョン最上層（弱） |
| 地下2階 | Y = -21〜-40 | |
| 地下3階 | Y = -41〜-60 | |
| 地下4階 | Y = -61〜-80 | |
| 地下5階 | Y = -81〜-100 | ダンジョン最下層（強） |

### 2.3 カメラ

- **視点**: 神の視点（クォータービュー・自由回転）
- **操作**: 画面中心のブロックを軸に移動・回転・ズーム
- キャラクター視点への切り替えは **スコープ外**（将来機能）

---

## 3. 宿屋システム

### 3.1 土地

- **初期面積**: 10m × 10m（地上グリッド上）
- **拡張単位**: 縦横 5m ずつ購入（資金消費）
- 拡張は隣接する未所有地への購入のみ可能

### 3.2 宿屋施設

- **ベッド**: 宿泊人数の上限を決定する主要設備
- **部屋**: ベッドを配置する空間単位
- **満足度への影響要素**:
  - 過密度（ベッド密度が高すぎると不満）
  - プライバシー（個室/相部屋の状況）
  - 設備の質（将来拡張用、現時点では未実装）

### 3.3 料金システム

- **基本料金**: 宿泊1回あたりの固定金額
- **チップ**: 満足度に応じて追加支払い（満足度が閾値を超えると発生）
- **不満時**: チップなし、基本料金のみ
- 満足度・料金のパラメータは `InnConfig` で外部設定

---

## 4. キャラクターシステム

### 4.1 クラス階層

```
CharacterBase (基底)
  ├── InnkeeperCharacter  (店長 = プレイヤー)
  ├── AdventurerCharacter (冒険者 = NPC)
  └── StaffCharacter      (店員 = NPC, 将来拡張)
```

### 4.2 CharacterBase パラメータ

| パラメータ | 型 | 説明 |
|-----------|-----|------|
| Hp | int | 現在HP |
| MaxHp | int | 最大HP |
| AttackPower | int | 攻撃力 |
| Defense | int | 防御力 |
| MoveSpeed | float | 移動速度 (m/s) |
| AttackSpeed | float | 攻撃速度 (回/秒) |

### 4.3 InnkeeperCharacter（店長）

- プレイヤーが操作する唯一のキャラクター
- 常に宿屋エリア内に存在
- 戦闘パラメータは実装するが、戦闘行為はスコープ外

### 4.4 AdventurerCharacter（冒険者）

- NPCとして自律行動
- 行動ループ: **宿屋で休憩 → ダンジョン探索 → 宿屋へ帰還** を繰り返す
- 宿屋のベッド空き数を上限として同時滞在数が決まる
- 満足度パラメータを個別に持つ（`CurrentSatisfaction`）

---

## 5. ダンジョンシステム

### 5.1 マップ生成

- **タイミング**: ゲーム開始時に全5階層を一括生成（固定シード or ランダム）
- **構造**: 迷路状（ランダム生成アルゴリズムは実装者に委ねる。変更可能な設計にする）
- **階段**: 各階に「上への階段」「下への階段」が最低1つずつ存在
  - 地下1階の上階段 → 地上への出口
  - 地下5階に下階段なし

### 5.2 敵モンスター

- 各階層内を常時徘徊（ウェイポイントベースまたはナビメッシュ）
- 基本的に階層をまたいで移動しない
- **強さ**: 地下が深くなるほど強くなる（階層インデックスに応じてパラメータスケール）
- パラメータ: CharacterBase と同じ構造を継承

### 5.3 冒険者のダンジョン行動

- 入口から入り、探索し、一定時間または体力消耗で帰還
- 戦闘の詳細ロジックは将来実装（現フェーズでは移動・徘徊まで）

---

## 6. AI設計方針

### 6.1 拡張性要件

将来的に以下をサポートできる設計とする:
- パーティ編成（複数冒険者の協調行動）
- AI賢さレベルの設定（慎重/普通/無謀 など）

### 6.2 設計方針

- 行動決定を `IAdventurerAI` インターフェースで抽象化
- デフォルト実装を提供しつつ、差し替え可能にする
- ステートマシンベースを推奨（StateパターンまたはHFSM）
- Unityの`NavMeshAgent`利用可、ただし将来的にカスタムに差し替え可能な設計にする

---

## 7. シーン構成（Lighthouse設計）

| SceneId | 種別 | 責務 |
|---------|------|------|
| `Launcher` | MainScene | 初期化・マスタデータロード・Worldへの遷移（既存シーン再利用） |
| `World` | MainScene | 地上の宿屋エリア＋ダンジョン全層・神の視点カメラ・メインゲームループ |
| `HUD` | ModuleScene | 資金・時刻・通知などのUI（World全体で共有） |
| `ScreenStackModal` | ModuleScene | ScreenStackのモーダル管理（全シーン共有） |

### 設計根拠

- `Gameplay` と `Dungeon` を `World` に統合: Lighthouse の ExclusiveSequence では同時アクティブな MainScene は1つのみ。冒険者が地上⇔地下をシームレスに行動するため、分離すると非アクティブ時にシミュレーションが停止する問題が生じる。
- `Launcher` を再利用: 既存の Launcher シーンが同等の責務を持つため Boot シーンは作成しない。
- `FirstScene` は削除済み（不要）。

---

## 8. スコープ外（MVP除外）

以下は将来実装。MVP では考慮するが実装しない:
- 戦闘の詳細ロジック（冒険者 vs モンスターの細かい演出）
- キャラクター個別のビジュアル・アニメーション
- セーブ/ロード
- BGM/SE
- 多言語対応（日本語固定）
- パーティシステム
- 店員NPCの行動

---

## 9. 設定ファイル一覧（マスタデータ対象）

| 設定名 | 主なパラメータ |
|--------|---------------|
| `WorldConfig` | マップサイズ・階層数・Y軸レンジ定義 |
| `InnConfig` | 基本料金・チップ閾値・初期土地サイズ・拡張単位 |
| `AdventurerConfig` | ベースステータス・満足度閾値・行動ループ時間 |
| `MonsterConfig` | 階層別ステータス倍率・徘徊パラメータ |
| `DungeonConfig` | 生成アルゴリズム種別・階段数最小値・シード設定 |
