# task_002: シーン構成整備

## 目的
Lighthouse のシーン構成を spec.md の確定設計（Launcher → World + HUD + ScreenStackModal）に
合わせて整備する。FirstScene を削除し、World / HUD シーンを追加する。

## 必読ドキュメント（作業前に必ず読む）
- docs/spec.md（セクション 7: シーン構成）
- docs/domain-design.md（セクション 2: フォルダ・名前空間設計）
- .claude/CLAUDE.md（コーディング規約・禁止事項）

## 現状の確認
- 既存シーン: Launcher.unity / FirstScene.unity / ScreenStack.unity
- 既存スクリプト（削除対象）: View/Scene/MainScene/QuickFirst/ 配下の 4 ファイル
- 自動生成ファイル（手動更新対象）: LighthouseGenerated/ 配下の 2 ファイル

---

## 作業 1: QuickFirst 関連ファイルの削除

以下のファイルをすべて削除する（.meta ファイルも削除すること）:

```
Client/Assets/DungeonInn/Runtime/Scripts/View/Scene/MainScene/QuickFirst/IQuickFirstPresenter.cs
Client/Assets/DungeonInn/Runtime/Scripts/View/Scene/MainScene/QuickFirst/QuickFirstLifetimeScope.cs
Client/Assets/DungeonInn/Runtime/Scripts/View/Scene/MainScene/QuickFirst/QuickFirstPresenter.cs
Client/Assets/DungeonInn/Runtime/Scripts/View/Scene/MainScene/QuickFirst/QuickFirstScene.cs
```

フォルダ `QuickFirst/` も削除する。

---

## 作業 2: SceneId ファイルの更新

### LighthouseGenerated/DungeonInnMainSceneId.g.cs
`FirstScene` を `World` に書き換える:

```csharp
// <auto-generated>
// This file is generated by SceneIdGenerator.
// DO NOT EDIT MANUALLY.
// </auto-generated>

using System;
using Lighthouse.Scene;

namespace DungeonInn.LighthouseGenerated
{
    public static class DungeonInnMainSceneId
    {
        public static readonly MainSceneId None = new MainSceneId(1, string.Empty);
        public static readonly MainSceneId World = new MainSceneId(2, "World");

        public static ReadOnlySpan<MainSceneId> All
        {
            get
            {
                return new MainSceneId[]
                {
                    None,
                    World,
                };
            }
        }
    }
}
```

### LighthouseGenerated/DungeonInnModuleSceneId.g.cs
`HUD` を追加する:

```csharp
// <auto-generated>
// This file is generated by SceneIdGenerator.
// DO NOT EDIT MANUALLY.
// </auto-generated>

using System;
using Lighthouse.Scene;

namespace DungeonInn.LighthouseGenerated
{
    public static class DungeonInnModuleSceneId
    {
        public static readonly ModuleSceneId None = new ModuleSceneId(1, string.Empty);
        public static readonly ModuleSceneId ScreenStack = new ModuleSceneId(2, "ScreenStack");
        public static readonly ModuleSceneId HUD = new ModuleSceneId(3, "HUD");

        public static ReadOnlySpan<ModuleSceneId> All
        {
            get
            {
                return new ModuleSceneId[]
                {
                    None,
                    ScreenStack,
                    HUD,
                };
            }
        }
    }
}
```

---

## 作業 3: SceneGroupProvider の更新

`Core/SceneGroupProvider.cs` を以下の内容に書き換える。
World シーンには ScreenStack と HUD の両方を割り当てる:

```csharp
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using DungeonInn.LighthouseGenerated;
using Lighthouse.Scene;

namespace DungeonInn.Runtime.Scripts.Core
{
    public sealed class SceneGroupProvider : ISceneGroupProvider
    {
        static readonly ModuleSceneId[] RequireSceneModuleIds =
        {
            DungeonInnModuleSceneId.ScreenStack,
        };

        static readonly IReadOnlyDictionary<MainSceneId, ModuleSceneId[]> SceneModuleMap =
            new Dictionary<MainSceneId, ModuleSceneId[]>
            {
                { DungeonInnMainSceneId.World, new[] { DungeonInnModuleSceneId.HUD } },
            };

        static readonly MainSceneId[][] MainSceneGroupList =
        {
            new[] { DungeonInnMainSceneId.World },
        };

        static readonly SceneGroup[] SceneGroupList = CreateSceneGroups();

        SceneGroup ISceneGroupProvider.GetSceneGroup(MainSceneId mainSceneId)
        {
            return SceneGroupList.First(sceneGroup => sceneGroup.MainSceneIds.Contains(mainSceneId));
        }

        static SceneGroup[] CreateSceneGroups()
        {
#if UNITY_EDITOR
            var duplicateMainScenes = MainSceneGroupList
                .SelectMany(x => x.Select(y => y))
                .GroupBy(x => x.Name)
                .Where(x => x.Count() != 1)
                .Select(x => x.Key)
                .ToArray();
            if (duplicateMainScenes.Any())
            {
                var duplicateSceneNames = string.Join(", ", duplicateMainScenes);
                throw new ConstraintException($"Duplicate scenes {duplicateSceneNames}");
            }
#endif

            return MainSceneGroupList
                .Select(CreateSceneGroup)
                .ToArray();
        }

        static SceneGroup CreateSceneGroup(MainSceneId[] mainSceneKeyList)
        {
            var sceneModuleDic = mainSceneKeyList.ToDictionary(
                mainSceneKey => mainSceneKey,
                mainSceneKey => RequireSceneModuleIds
                    .Concat(SceneModuleMap[mainSceneKey] ?? Array.Empty<ModuleSceneId>()).ToArray());

            return new SceneGroup(sceneModuleDic);
        }
    }
}
```

---

## 作業 4: World MainScene スクリプトの作成

格納先: `Client/Assets/DungeonInn/Runtime/Scripts/View/Scene/MainScene/World/`

### WorldScene.cs

```csharp
using Cysharp.Threading.Tasks;
using DungeonInn.LighthouseGenerated;
using DungeonInn.Runtime.Scripts.View.Base;
using Lighthouse.Scene;
using VContainer;

namespace DungeonInn.Runtime.Scripts.View.Scene.MainScene.World
{
    public class WorldScene : ProductCanvasMainSceneBase<WorldScene.WorldTransitionData>
    {
        public override MainSceneId MainSceneId => DungeonInnMainSceneId.World;

        public class WorldTransitionData : ProductTransitionDataBase
        {
            public override MainSceneId MainSceneId => DungeonInnMainSceneId.World;
        }
    }
}
```

### WorldLifetimeScope.cs

```csharp
using VContainer;
using VContainer.Unity;

namespace DungeonInn.Runtime.Scripts.View.Scene.MainScene.World
{
    public class WorldLifetimeScope : LifetimeScope
    {
        protected override void Configure(IContainerBuilder builder)
        {
            // Phase 2 以降でユースケース・AI・Presenter を登録する
        }
    }
}
```

---

## 作業 5: HUD ModuleScene スクリプトの作成

格納先: `Client/Assets/DungeonInn/Runtime/Scripts/View/Scene/ModuleScene/HUD/`

### HudModuleScene.cs

```csharp
using DungeonInn.LighthouseGenerated;
using DungeonInn.Runtime.Scripts.View.Base;
using Lighthouse.Scene;

namespace DungeonInn.Runtime.Scripts.View.Scene.ModuleScene.HUD
{
    public class HudModuleScene : ProductCanvasModuleSceneBase
    {
        public override ModuleSceneId ModuleSceneId => DungeonInnModuleSceneId.HUD;
    }
}
```

### HudLifetimeScope.cs

```csharp
using VContainer;
using VContainer.Unity;

namespace DungeonInn.Runtime.Scripts.View.Scene.ModuleScene.HUD
{
    public class HudLifetimeScope : LifetimeScope
    {
        protected override void Configure(IContainerBuilder builder)
        {
            // Phase 2 以降で HUD の Presenter を登録する
        }
    }
}
```

---

## 作業 6: Launcher の更新

`Core/Launcher.cs` の `TransitionNextScene()` を World へ遷移するよう書き換える。
QuickFirst の using は削除する:

```csharp
using Cysharp.Threading.Tasks;
using DungeonInn.Runtime.Scripts.View.Scene.MainScene.World;
using Lighthouse.Scene;
using UnityEngine;
using VContainer;

namespace DungeonInn.Runtime.Scripts.Core
{
    public sealed class Launcher : ILauncher
    {
        static readonly string LauncherSceneName = "Launcher";

        readonly ISceneManager sceneManager;

        [Inject]
        public Launcher(ISceneManager sceneManager)
        {
            this.sceneManager = sceneManager;
        }

        void ILauncher.Reboot()
        {
            RebootProcess().Forget();

            async UniTask RebootProcess()
            {
                await sceneManager.PreReboot();
                await UnityEngine.SceneManagement.SceneManager.LoadSceneAsync(LauncherSceneName, UnityEngine.SceneManagement.LoadSceneMode.Single);
                await LaunchProcess();
                TransitionNextScene();
            }
        }

        async UniTask ILauncher.Launch()
        {
            await FirstLaunchProcess();
            await LaunchProcess();
            TransitionNextScene();
        }

        UniTask FirstLaunchProcess()
        {
            return UniTask.CompletedTask;
        }

        UniTask LaunchProcess()
        {
            return UniTask.CompletedTask;
        }

        void TransitionNextScene()
        {
            UniTask.Void(async () =>
            {
                await sceneManager.TransitionScene(new WorldScene.WorldTransitionData());

                if (!string.IsNullOrEmpty(UnityEngine.SceneManagement.SceneManager.GetSceneByName(LauncherSceneName).name))
                {
                    await UnityEngine.SceneManagement.SceneManager.UnloadSceneAsync(LauncherSceneName);
                }
            });
        }
    }
}
```

---

## 作業 7: Unity シーンファイルの作成

`uloop execute-dynamic-code` を使い、Unity Editor API でシーンを生成する。

### World.unity の作成

以下の C# コードを `uloop execute-dynamic-code` で実行する:

```csharp
#if UNITY_EDITOR
using UnityEditor;
using UnityEditor.SceneManagement;
using UnityEngine;

var scenePath = "Assets/DungeonInn/Runtime/Scene/MainScene/World.unity";
var scene = EditorSceneManager.NewScene(NewSceneSetup.EmptyScene, NewSceneMode.Single);
var go = new GameObject("WorldScene");
EditorSceneManager.SaveScene(scene, scenePath);
AssetDatabase.Refresh();
Debug.Log($"Created scene: {scenePath}");
#endif
```

### HUD.unity の作成

以下の C# コードを `uloop execute-dynamic-code` で実行する:

```csharp
#if UNITY_EDITOR
using UnityEditor;
using UnityEditor.SceneManagement;
using UnityEngine;

var scenePath = "Assets/DungeonInn/Runtime/Scene/ModuleScene/HUD.unity";
var scene = EditorSceneManager.NewScene(NewSceneSetup.EmptyScene, NewSceneMode.Single);
var go = new GameObject("HudModuleScene");
EditorSceneManager.SaveScene(scene, scenePath);
AssetDatabase.Refresh();
Debug.Log($"Created scene: {scenePath}");
#endif
```

---

## 作業 8: EditorBuildSettings への追加

`uloop execute-dynamic-code` でシーンをビルド設定に追加する:

```csharp
#if UNITY_EDITOR
using UnityEditor;

var scenes = EditorBuildSettings.scenes;
var newScenes = new EditorBuildSettingsScene[]
{
    new EditorBuildSettingsScene("Assets/DungeonInn/Runtime/Scene/Launcher.unity", true),
    new EditorBuildSettingsScene("Assets/DungeonInn/Runtime/Scene/MainScene/World.unity", true),
    new EditorBuildSettingsScene("Assets/DungeonInn/Runtime/Scene/ModuleScene/ScreenStack.unity", true),
    new EditorBuildSettingsScene("Assets/DungeonInn/Runtime/Scene/ModuleScene/HUD.unity", true),
};
EditorBuildSettings.scenes = newScenes;
AssetDatabase.SaveAssets();
Debug.Log("Build settings updated.");
#endif
```

---

## 作業 9: FirstScene.unity の削除

`uloop execute-dynamic-code` で削除する:

```csharp
#if UNITY_EDITOR
using UnityEditor;

AssetDatabase.DeleteAsset("Assets/DungeonInn/Runtime/Scene/MainScene/FirstScene.unity");
AssetDatabase.Refresh();
Debug.Log("Deleted FirstScene.unity");
#endif
```

---

## 完了条件

1. `uloop.cmd compile --project-path Client` でエラーゼロ
2. `review/task_002_done.md` に以下を記載:
   - 作成・更新・削除したファイル一覧
   - コンパイル結果
   - 懸念点（あれば）
